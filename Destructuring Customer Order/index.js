let order = {
  orderId: "123456",
  productName: "Laptop",
  quantity: 2,
};

const { orderId, productName, quantity } = order;
console.log(
  `Order ID: ${orderId}, Product Name: ${productName}, Quantity: ${quantity}`
);
